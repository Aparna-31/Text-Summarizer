# 🧠 LLM Text Summarizer using HuggingFace-Mini Project

This project demonstrates a simple but effective **text summarization tool** using  
HuggingFace’s `facebook/bart-large-cnn` model.

## 🚀 What this project does
- Loads a pre-trained transformer model  
- Takes **user input** (any long text)  
- Generates a clean summary  
   

---

## 🧩 Technologies Used
- Python  
- HuggingFace Transformers  
- BART LLM (facebook/bart-large-cnn)  
- Jupyter Notebook  

---

## 📌 How to Run the Project
Install required libraries:

```bash
pip install transformers sentencepiece torch
